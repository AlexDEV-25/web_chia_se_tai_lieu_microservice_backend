spring:
  application:
    name: statistics-service

  main:
    banner-mode: off

server:
  port: 8087

logging:
  level:
    root: warn

app:
  services:
    auth:
      url: ${AUTH_SERVICE_URL}
    study:
      url: ${STUDY_SERVICE_URL}